import chai from 'chai';
const expect = chai.expect;

describe('See if the customers tests are running', () => {
  it.skip('should return true', function () {
    expect(true).to.equal(true);
  });
});
